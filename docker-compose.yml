services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: openrag
    ports:
      - "5432:5432"
    volumes:
      - openrag_pg_data:/var/lib/postgresql/data

volumes:
  openrag_pg_data:
